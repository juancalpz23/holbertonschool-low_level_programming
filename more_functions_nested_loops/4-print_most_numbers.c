#include "main.h"
/**
 * print_most_numbers - Prints from 0 to 9 except 2 and 4
 *
 * Return: Void
 **/
void print_most_numbers(void)
{
	int x;

	for (x = 0; x <= 0; x++)
	{
		if (x == 2 || x == 4)
		{
			continue;
		}
		_putchar(x + '0');
	}
}
