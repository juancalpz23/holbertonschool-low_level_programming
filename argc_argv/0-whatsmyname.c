#include "main.h"
/**
 * main - Prints its name
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	int i = 0;

	if (i < argc)
	printf("%s\n", argv[i]);

	return (0);
}
