#include "main.h"
/**
 * add - Adds two integer and returns the result
 *@x: variable to be added
 *@y: other variable to be added
 * Return: result 'z'  Success
 **/
int add(int x, int y)
{
	int z = x + y;

	return (z);
}
